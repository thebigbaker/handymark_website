$( document ).ready(function(){

  // Navbar
  $(".button-collapse").sideNav();

  // Scrollspy
  $('.scrollspy').scrollSpy();

  // Reviews
  $('.carousel.carousel-slider').carousel({fullWidth: true});
})